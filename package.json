{
  "name": "mcp-gdrive-server",
  "description": "MCP server for interacting with Google Drive",
  "author": "naresh",
  "version": "0.1.0",
  "license": "ISC",
  "type": "module",
  "bin": {
    "mcp-server-gdrive": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "start": "node --env-file=.env dist/index.js auth"
  },
  "dependencies": {
    "@google-cloud/local-auth": "^3.0.1",
    "@modelcontextprotocol/sdk": "^1.10.1",
    "googleapis": "^148.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "shx": "^0.4.0",
    "typescript": "^5.8.3"
  }
}
